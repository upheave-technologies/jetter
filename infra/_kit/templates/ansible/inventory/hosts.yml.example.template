all:
  children:
    app_servers:
      hosts:
        __APP_SLUG__-prod:
          ansible_host: YOUR_VPS_IP
          ansible_user: root  # First run as root, change to deploy after
          ansible_ssh_private_key_file: ~/.ssh/id_rsa  # Path to your SSH private key
