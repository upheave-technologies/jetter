POSTGRES_PASSWORD={{ vault_postgres_password | replace('$', '$$') }}
# Add your app-specific env vars below — reference vault variables declared in
# group_vars/all/vault.yml (see vault.yml.example for the naming convention).
# Example:
# __APP_SLUG_UPPER___API_KEY={{ vault___APP_SLUG___api_key }}
