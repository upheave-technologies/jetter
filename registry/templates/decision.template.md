---
id: DEC-000
title: <short outcome, e.g. "Modules never import each other">
status: proposed          # proposed | accepted | superseded | deprecated
decided: 2026-01-01
owner: <name>
supersedes: []            # optional — DEC ids this one replaces, e.g. [DEC-003]
relates: []               # optional — INIT ids this decision binds, e.g. [INIT-001, INIT-004]
---

## Context

The situation that called for this decision. What problem appeared, why the existing answer was insufficient, what constraints applied. One or two paragraphs — enough that a future reader can reconstruct the trade-space without prior knowledge.

## Decision

What was decided, stated as a positive claim. Imperative voice ("Modules never import each other") beats descriptive voice ("It was decided that modules should not import each other"). One paragraph is usually enough.

## Consequences

What now becomes true. What gets easier and what gets harder. What we will re-evaluate, and under what trigger. Bullet form is fine. Be honest about the costs — a decision without acknowledged downsides is one nobody trusts.

- **Easier**: …
- **Harder**: …
- **Revisit when**: …
